"""``agentrace benchmark`` — run bundled suite (web / code / RAG) presets.

TODO: select suite module; pass through parallelism and storage flags.
"""

# TODO: def register_benchmark_command(app: typer.Typer) -> None
