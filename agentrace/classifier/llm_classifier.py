"""Stage 2 classification: structured-output LLM call (via litellm).

TODO: pydantic-free parsing: JSON schema + manual validation or typed dict.
"""

# TODO: async def classify_with_llm(...) -> list[FailureRecord]
