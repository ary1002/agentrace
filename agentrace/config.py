"""YAML-backed application configuration via pydantic-settings (eval.yaml schema).

TODO: define ``EvalSettings`` (or equivalent) loading ``eval.yaml`` / env overrides.
Core package datamodels remain stdlib dataclasses; only this module uses Pydantic.
"""

# TODO: from pydantic_settings import BaseSettings, SettingsConfigDict
